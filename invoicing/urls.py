from django.urls import path
from . import views

app_name = 'invoicing'

urlpatterns = [
    # Archivo eliminado. Las rutas de facturación ahora se gestionan desde accounting o han sido migradas.
]
